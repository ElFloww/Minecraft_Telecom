{
  "name": "telecom-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "check": "node --check main.js && node --check coverage.js && node --check map-image.js",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "vite": "6.4.3"
  }
}
